# launchd/run.sh가 source하는 비밀값 파일.
# launchd/run.env로 복사 후 값 채우기. run.env 자체는 .gitignore로 추적 제외.

# 컨트롤 페이지 로그인 비밀번호 (필수).
CONTROL_PASSWORD=changeme

# 세션 쿠키 서명용 secret (선택). 비워두면 backend/runtime/.secret_key 자동 생성.
# CONTROL_SECRET_KEY=
